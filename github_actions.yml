# github_actions.yml

name: Sweep GHA Fix
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2.3

      # Update the Trunk Check setup instructions
      - name: Set up Trunk Check
        run: |
          # Provide detailed instructions for setting up Trunk Check
          # Add specific setup instructions for Trunk Check here

      - name: Install dependencies
        uses: trunk-io/trunk-action@v4
        run: npm ci

      - name: Run tests
        run: npm test

      - - name: Set up Trunk Check
        run: |
          # Provide detailed instructions for setting up Trunk Check
          # Reference the README.md file for detailed instructions
          # on how to set up the GitHub Actions integration

          # Detailed setup instructions for Trunk Check:
          # Add your detailed setup instructions here
        run: |
          # Provide detailed instructions for setting up Trunk Check
          # Reference the README.md file for detailed instructions
          # on how to set up the GitHub Actions integration

          # Detailed setup instructions for Trunk Check:
          # Add your detailed setup instructions here
      - name: Set up Node.js
        # Remove duplicate 'uses: actions/setup-node@v3' step
        with:
          node-version: 14
        uses: actions/setup-node@v3
        with:
          node-version: 14
  - name: Install dependencies
            uses: trunk-io/trunk-action@v4
        - name: Run tests
          run: npm test
        run: npm test
        run: npm test
