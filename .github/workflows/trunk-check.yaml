name: Trunk Check
run-name: Trunk-Check-Workflow
on:
  workflow_dispatch:
    inputs:
      payload:
        description: For invocation by Trunk Check
        description: Trigger for Trunk Check
        required: true

permissions:
  contents: read
  actions: read

jobs:
  check:
    name: ${{ fromJSON(inputs.payload).checkJobName }}
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ fromJSON(inputs.payload).concurrencyGroup }}
      cancel-in-progress: true

    steps:
      - name: Trunk Check
        uses: trunk-io/trunk-action@v1
        with:
          env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          yarn install
          yarn build
          yarn test
          yarn capture-logs > error_logs.txt
          echo "Error Logs" > error_logs.txt
          cat error_logs.txt > captured_error_logs.txt
          echo "Captured error logs:"
          yarn install
          yarn build
          yarn test
          yarn capture-logs > error_logs.txt
          echo "Error Logs" > error_logs.txt
          cat error_logs.txt > captured_error_logs.txt
          echo "Captured error logs:"
          npm install @trunk/actions
          npx trunk check
